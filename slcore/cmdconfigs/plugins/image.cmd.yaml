unpack:
    help: "Plugin - Unpack firmware."
    optional: &optional
      firmware: {short: f, metavar: "FIRMWARE", required: True,
            help: "We extend Binwalk to extract more Linux kernel in firmware."}
      show: {short: s, help: 'Show components.', action: 'store_true', default: True} 
    callback: unpack
