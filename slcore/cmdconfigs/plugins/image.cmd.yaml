unpack:
    help: "Plugin - Unpack an image."
    optional: &optional
        ffirmware: {short: ff, metavar: "IMAGE",
            help: "We extend Binwalk to identify more image headers and can extract the Linux kernel in the image."}
    callback: unpacking
